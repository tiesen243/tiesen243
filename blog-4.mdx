---
title: Blog 4
description: This is a blog post. You can replace this content with your own.
tags: [blog, post]
image: /images/design/4.png
date: 2024-02-18T04:04:21.000Z
---

## Table of Contents

1. [Heading 1](#heading-1)
2. [Heading 2](#heading-2)
3. [Heading 3](#heading-3)


## Heading 1 

This is a blog post. You can replace this content with your own. 

## Heading 2 

This is a blog post. You can replace this content with your own. 

## Heading 3 

This is a blog post. You can replace this content with your own. 

```python
class Perceptron:
  def __init__(self, num_features):
    self.weights = np.zeros(num_features)
    self.bias = 0
    self.learning_rate = 0.05
  
  def predict(self, x):
    linear = np.dot(x, self.weights) + self.bias
    return 1 if linear > 0 else 0 

  def fit(self, X, y):
    for _ in range(100):
      for i in range(len(X)):
        y_pred = self.predict(X[i])
        update = self.learning_rate * (y[i] - y_pred)
        self.weights += update * X[i]
        self.bias += update
```


