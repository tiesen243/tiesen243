---
title: "R Notebook"
date: "2017-11-30"
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

## Python

```{python}
import numpy as np

def square(x):
    return x * x

x = np.random.randint(1, 10)
y = square(x)
print('%d squared is %d' % (x, y))
```

```{cpp}
#include <iostream>
using namespace std;

int main() {
    cout << "Hello World!";
    return 0;
}
```
